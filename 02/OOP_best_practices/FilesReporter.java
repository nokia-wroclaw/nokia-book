import java.io.File;

public class FilesReporter {

	public File[] listFiles(String folderPath, boolean isRecursive) {
		if (isRecursive) {
			// code for listing files from folderParth recursively
		} else {
			// code for listing files from folderParth only
		}
	}

}
